# Prerequisites
*.d

# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Linker files
*.ilk

# Debugger Files
*.pdb

# Compiled Dynamic libraries
*.so
*.dylib
*.dll
*.so.*

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# Build directories
build/
Build/
build-*/

# CMake generated files
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
install_manifest.txt
compile_commands.json

# Temporary files
*.tmp
*.log
*.bak
*.swp

# debug information files
*.dwo

# test output & cache
Testing/
.cache/

# --- cheatah-plot ---
# CPM's downloaded bootstrap (the committed cmake/CPM.cmake stays; the versioned copy is fetched).
cmake/CPM_*.cmake
# Clang source-based coverage scratch.
*.profraw
*.profdata
# Compiled shaders (Slang -> SPIR-V is produced on demand by the build/doctor).
*.spv
# Generated system-test helper header (qa_gate emits check.purr to a temp dir; guard stray copies).
systests/check.hpp
systests/check.hpp.sha512
# The plot.* module SOURCES are the committed `.purr`; the generated `.hpp` (+ .sha512 sidecars) are
# also committed (that is the biome-shipped artifact) and kept in sync by scripts/gen-headers.sh.

# Local Claude Code session settings (not part of the library).
.claude/

# systest render outputs (written to the repo root by the qa_gate stage-5 runs)
out_*.purr.png
